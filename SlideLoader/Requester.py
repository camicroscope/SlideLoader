# use requests if it's thread safe

class Requester(object):
    # using options...
    # check the check route
    # log if present
    # if not, post to post route
    # log if error
    # (error log should be usable as a retry manifest)
