from SlideLoader import Requester
# test cases for requester


# are there thread safety tests? would that go in controller's tests?
# will not post if the checkroute condition returns
# will post if it does not
# uri encodes values in uri
# body isn't messed up
